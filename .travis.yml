sudo: required
services: docker
language: clojure
before_install:
  - docker pull clojusc/mesos:1.0.1
  - docker run -d -p 5050:5050 clojusc/mesos:1.0.1
script:
  - lein travis
dist: trusty
jdk:
  - oraclejdk8
  #- oraclejdk7
  #- openjdk6
  #- openjdk7

notifications:
  email:
      - oubiwann@gmail.com
